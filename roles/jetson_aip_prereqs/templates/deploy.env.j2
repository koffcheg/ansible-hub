# Managed by Ansible - AIP deploy configuration

{% if aip_blob_name %}
BLOB_NAME={{ aip_blob_name }}
{% else %}
# BLOB_NAME is intentionally empty here.
# Set 'aip_blob_name' in host_vars/group_vars before real deploys.
BLOB_NAME=
{% endif %}
