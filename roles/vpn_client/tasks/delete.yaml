- name: Remove client config folder
  file:
    path: "client-configs/{{ client_name }}"
    state: absent

- name: Delete secret from GCP
  gcp_secret_manager_secret:
    name: "openvpn_{{ client_name }}"
    state: absent
  delegate_to: localhost